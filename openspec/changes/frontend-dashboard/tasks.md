## 0. 基础设施

- [x] 0.1 路由/ico/BaseView 骨架(8/14)。
- [x] 0.2 `types/dev.ts` 全套类型 + `axios/dev.ts` 8 端点(8/14):契约对齐后端序列化规则(枚举 `name()`、UUID→string、`non_null` 下可空字段标 `?:`),返回类型 `AxiosResponse<R<T>>`,无 data 端点标 `R<void>`;拦截器死代码修复(`code !== SUCCESS` + `msg`)。
- [x] 0.3 `GET /dev/run` 列表接口(8/15):`projectId`/`adminId` 可选过滤、都不带查全量、按 `create_time` 倒序,读公开;`dev_run` 无 admin_id 列,adminId 过滤先查归属项目再 `in`。axios 加 `getAllRun`。**8/17 分页化**:`pageNum`/`pageSize` 必填(`@Positive`,照 blog 模块范式),返回 `PageResult`(MyBatis-Plus `selectPage`,分页拦截器早已配置);另加可选 `statuses` 过滤(`in`,前端逗号拼接、Spring 逗号分隔绑定 `List<Run.Status>`)——分页后"最近完成/历史任务"等终态视图不能再靠前端正过滤(进行中的 run 会混入首页造成缺行/空页),statuses 是正确性配套而非额外守卫;`GET /dev/project` 列表同步分页(按 `create_time` 倒序,稳定分页需确定序)。**8/17 再简化**:`adminId` 过滤无任何调用方(我的项目页按 `projectId` 查、平台运维查全量、项目子菜单走 project 接口前端筛),连同"先查归属项目再 `in`"段一并删除——run 列表最终签名 `projectId? + statuses? + pageNum + pageSize`。axios `getAllRun`/`listProject` 随迁 `PageResult`。
- [x] 0.4 `GET /dev/admin/stats` 平台统计(8/15):`pojo/bo/Stats`(projectCount/enabledProjectCount/executingRunCount/pendingRunCount/maxConcurrency/deliveredIssueCount/prTotal/prMerged——占槽分母 maxConcurrency 来自配置 `morningstar.app.dev.schedule.max-concurrency`,面板展示必需的唯一配置项;百分比/占槽比均由前端算,后端只出原始计数与配置),读公开不加 `@PreAuthorize`(统计是"看数"非"操作",不发明第三个权限点);修复口径 = **属于 status=SUCCEEDED 的 run** 且 issue 状态 VERIFIED/ACCEPTED/REJECTED(失败/取消的 run 会 Restore 回滚,其 FIXED/VERIFIED issue 未交付,不计入)。axios 加 `getDevStats`。8/16 补两字段:`acceptedIssueCount`(已采纳,全局 ACCEPTED issue 计数)与 `savedPersonDays`(累计节约人天,Double;`IssueMapper.calcSavedPersonDays()` 自定义 XML——`ROUND(COALESCE(SUM(effort_in_minutes),0)/480.0,1)` 一步换算,480.0 带小数防整除截断,COALESCE 兜零行;写死 'ACCEPTED' 无参数,"节约人天"概念只绑已采纳)。8/16 再补两字段:`scheduledStartTime`/`scheduledEndTime`(LocalTime,源自配置 `create-cron`/`cleanup-cron`,`AdminServiceImpl.convertCronToLocalTime` 取 cron 时/分段;`JsonUtil` 补 LocalTime 模块按 ISO `HH:mm:ss` 序列化)——展示位置 8/16 晚改定:**顶栏导航右侧常驻胶囊**(BaseView 拉一次 stats,钟表图标 + "夜间调度 HH:mm–HH:mm",橙色浅底 `bg-orange-50` 配主题——8/16 晚由深灰 `bg-slate-800` 调浅,用户评深色太别扭,三个 dev 页面共享;曾放平台运维"当前任务"卡头,用户评位置不合理改顶栏)。
- [x] 0.5 `ProjectDetail` 展示扩充(8/15):PO `Project` 不加展示字段,bo `ProjectDetail extends Project`(`@SuperBuilder`)补 `adminName`(取项目管理员 `username`,唯一登录名辨识度高;nickname 可选常为空,不做 fallback 链);`ProjectService` 所有返回 Project 的方法统一经 `toDetail` 扩充(单条转换为正典,批量即 `stream().map(this::toDetail)`,MVP 不做批量查询优化),Controller 返回类型随迁;前端同步形态:`ProjectDetail extends Project`(`adminName?: string`),axios 项目端点返回类型全部用 `ProjectDetail`。
- [x] 0.6 `RunDetail` 展示扩充(8/15):同 0.5 思路,bo `RunDetail extends Run` 补 `projectName`(项目可能已删而 run 是日志型数据不随删,归属项目为 null 时字段留空降级)与 `prLink`(复用 `GiteaUtil.getPrLink` 拼 `backendOrigin/owner/repo/pulls/{prId}`,与 PR body 文件链接同 origin 口径;`project == null` 或 `prId == null` 时留空降级)及 `deliveredIssueCount`(**交付口径,同 Stats.deliveredIssueCount**:仅 `status=SUCCEEDED` 的 run 非 null,值为该 run 下 VERIFIED/ACCEPTED/REJECTED issue 总数;失败/取消的 run 会 Restore 回滚,其 issue 未交付,字段留 null);`RunService` 出口统一经 `toDetail` 扩充;前端 `RunDetail extends Run`(`projectName?: string` + `prLink?: string` + `deliveredIssueCount?: number`),axios run 端点随迁。
- [x] 0.7 `Run.triggerType` 触发方式(8/15):PO `Run` 加嵌套枚举 `TriggerType { MANUAL, SCHEDULED }` + 字段 `triggerType`(列 `trigger_type`,`trigger` 是 MySQL 保留字;枚举不叫 `Trigger` 避免与状态机 `statemachine/Trigger` 接口撞名);`createRun(projectId, triggerType)` 由调用方声明——`triggerRun` 传 `MANUAL`、`CronTask` 传 `SCHEDULED`;真实属性进 PO 而非 Detail;前端 `RunTriggerType` 枚举 + `Run.triggerType`,展示映射 MANUAL→手动/SCHEDULED→调度(8/16 晚定稿:列头已含"触发方式",值不再重复"触发"二字)。
- [x] 0.8 UI 原型定稿(8/15,`frontend/prototype/dev.html`,Vue3 CDN + Tailwind):浅色橙主题(底 `#f3f4f6`、白卡 slate-200 边、主色 orange-500、语义浅底徽章 emerald/rose/amber/violet);三页 IA、吸顶二级子菜单、通栏布局、回退环等交互均以原型为准,真实页面照原型实现。
- [x] 0.9 `RunDetail.actionAttemptBriefs` 阶段流水(8/15):bo `ActionAttemptBrief`(`actionType/attemptNo/status/createTime/updateTime`,不含 `result`——ScanResult 带全量 issueKeys 体积大;查询层列裁剪使 result 列不出库),`toDetail` 统一填充(getRun/listRun/triggerRun 出口一致;cancelRun 两处仅需 PO 鉴权,改为自己 `selectById` 不借 getRun);前端画图口径:节点耗时 = attempt 的 createTime→updateTime,阶段失败徽章 = FAILED 按 actionType 分组计数,回退环激活次数 = RESTORE 条数。配套 `CopyUtil` 健壮化(infra):泛型签名改 `Object`、两侧沿父类链按名字匹配字段(跨类 PO→Brief 拷贝安全、target 缺字段跳过、同名遮蔽只留子类、static/synthetic 不拷);前端 `types/dev.ts` 加 `ActionType`/`ActionStatus` 枚举与 `ActionAttemptBrief` 接口。
- [x] 0.10 `RunDetail` 漏斗四值(8/16):补 `scannedIssueCount`(扫描发现——`actionAttemptMapper.selectLatestActionAttempt(runId, SCAN)` 拿最新 SCAN attempt,result 经 typehandler 反序列化为 `ScanResult`,SUCCEEDED 时取 `scannedSonarIssueNum + scannedAiIssueNum`,未扫描/失败留 null)/`selectedIssueCount`(本轮入选——本 run issue 总行数)/`currentFixedIssueCount`(已修复——状态 FIXED 及之后的累计口径,含 VERIFIED/ACCEPTED/REJECTED,漏斗单调不减)/`currentVerifiedIssueCount`(验证通过——VERIFIED 及之后);实现为一次只 select status 列的查询 + Java 计数,`deliveredIssueCount` 复用 currentVerifiedIssueCount 不再单独 count;前端 `types/dev.ts` 同步四个可选字段。

## 1. 我的项目(`/dev`)

- [x] 1.1 项目切换为吸顶二级子菜单(项目胶囊,按登录用户 id 匹配 `adminId` 过滤;与顶栏同一 sticky 容器)+"接入新项目"弹窗表单(复用配置校验,管理员灰显当前用户,创建后自动选中)。(8/16,`ProjectView` + `ProjectFormModal`)
- [x] 1.2 项目卡:链接/分支/enabled/上限/管理员展示;owner 操作(编辑配置弹窗、手动触发、启停、删除);非 owner 只读。(8/16;落地为三行规格表:项目描述/仓库信息(链接 owner/repo 短形可点跳 + 分支)/配置信息(单轮任务处理问题上限 + 调度启停)(窄屏策略 8/16 晚:信息区 flex-1 + min-w-280px;按钮组 xl 断点切换——xl 以下直接 2×2 等宽网格(`grid-cols-2 xl:grid-cols-4`),不做逐按钮掉落,不挤压信息区),**管理员不展示**——子菜单已按 adminId 过滤只列自己的项目,"非 owner 只读"视图不存在;**8/16 按钮措辞定稿**:启停按钮为"停用调度/启用调度"(四字明示切换的是夜间调度而非项目本身,与横幅"调度启停 启用/停用"呼应),删除按钮为"删除项目")
- [x] 1.3 当前任务状态机可视化(有活跃 run 才渲染):7 节点流水线 + 当前节点高亮 + 耗时 + 当前阶段说明一行,轮询更新;头部阶段失败徽章(如"同步失败 ×1",数据源 `actionAttemptBriefs`(0.9):FAILED 按 actionType 分组计数,前端聚合,无需专用接口);流水线下方回退环弧线(验证→修复,RESTORING 是节点间回路不是节点:休眠淡虚线、激活虚线流动动画);漏斗计数条"扫描发现问题 / 本轮入选 / 已修复 / 验证通过"(四值数据源 = `RunDetail` 漏斗四字段,0.10 已就绪:scannedIssueCount/selectedIssueCount/currentFixedIssueCount/currentVerifiedIssueCount);满槽触发停留 PENDING 时展示"排队中"(决策 46)。(8/16,`PipelineStateMachine`;补充定稿:统一 **3s** 轮询——**8/17 分页化后拆两路**:活跃快照(pageSize=1)喂状态机/漏斗、当前历史页喂历史表,同 3s 节奏;"已耗时"为本地 1s 秒表,与轮询解耦,`fmtElapsed` 秒级 H:MM:SS(8/16 晚定稿,与历史表 `fmtDuration`"X.X 分"口径区分);扫描发现问题显示真实数值(8/16 晚定稿,弃 99+ 折叠——"反映真实就很好");四值 SCAN 成功为闸,此前 null 显示 —;**8/16 晚改为常驻渲染**——`run` 可选,无活跃 run 时渲染空闲骨架:节点全灰、漏斗全 —、头部"暂无进行中的任务"、阶段说明引导手动触发;节点图标定稿(8/16 晚):未到达/当前节点内嵌专属 antd 图标(见 spec),已完成统一打勾,图标归属写进 `PIPELINE_NODES[].icon`;流水线区响应式策略(8/16 晚定稿):不做重排适配,容器 720px 最小宽度(7×48 节点 + 6×64 连接线)+ `overflow-x-auto`,窄屏横向滚动;头部右侧 `actions` 插槽放"取消"按钮(复用 `cancelRun` 端点,确认弹窗,PENDING 排队中不显示);CANCELING 取消态视觉:当前节点/阶段标题转琥珀 + 头部右侧"取消中"徽章(8/16 晚挪到右侧操作组,与平台运维卡头同位置),取消按钮仅 status=RUNNING 时显示(管理页迷你条同口径(琥珀)))
- [x] 1.4 历史任务表(单表通栏,PR 不拆表——`prId`/`prStatus` 本就是 run 的列):任务 id/结果/触发方式(`triggerType`,0.7)/修复数(`deliveredIssueCount`,0.6;非 SUCCEEDED 为 null 显示 —)/PR(有则渲染 `prLink` 链接(后端已拼好,0.6) + prStatus 徽章,无则 —)/开始时间(`createTime`)/结束时间(终态 run 的 `updateTime`)/耗时(前端算差值)。(8/16,`RunHistoryTable`,PR 新标签打开;**8/16 晚重排定稿**:列=任务编号(id 前 8 位等宽居中,title 全 id,与管理页运行卡同口径 8 位)/触发方式(独立列居中,值=手动/调度)/运行结果(徽章居中)/扫描发现问题数(居中,`scannedIssueCount` 真实数值,未扫描 —,title 悬浮"本次任务扫描发现的问题总数(SonarQube + AI)")/已交付修复数(居中,`deliveredIssueCount`,非 SUCCEEDED —)/PR(居中)/开始时间(居中)/结束时间(居中)/任务耗时;对齐规则=除耗时右对齐外全部居中;`table-fixed` + colgroup 固定列宽(11/9/9/11/11/13/13/13/10,近似均分,时间列不额外加宽);xl 以下隐藏开始/结束时间两列(col/th/td 同步 `hidden xl:table-column|table-cell`,8/16 晚定稿);**8/16 晚加"扫描发现问题"列**——用户认为历史表信息太少,扫描发现问题/已交付修复构成"发现 → 交付"递进,漏斗其余三值(本轮入选/已修复/验证通过)仍只在当前任务区块展示;**8/17 服务端分页**:历史任务 = 终态 statuses 过滤 + 每页 10 条(活跃 run 不再靠前端 `filter` 剔除),活跃 run 快照(pageSize=1 取最新一条判定)与历史当前页两路 3s 轮询;`PageSwitcher` 放"历史任务"标题行右侧(8/17 晚由表底挪入,‹ 当前页 › 三元素,浅灰 hover 橙,仅一页时组件自隐藏,末页抽空自动回退一页))

## 2. 平台运维页(`/dev/admin`)

- [x] 2.1 KPI 行(来自 `getDevStats`,0.4):接入仓库数(附"N 个启用中"=`enabledProjectCount`)/累计交付修复数(附"N 个已采纳"=`acceptedIssueCount`)/累计节约人天(`savedPersonDays`,按已采纳修复的估算工时)/PR 合并率(`prMerged`/`prTotal` 算百分比,附"n/m 已合并")/并发任务(`executingRunCount`/`maxConcurrency`,附排队中 `pendingRunCount`);与"当前任务"列表同口径(排除 PENDING)。(8/16,`AdminView`;**顺序定稿:接入仓库 → 并发任务 → 累计交付修复 → PR 合并率 → 累计节约人天(压轴)**;KPI/项目/当前任务/最近完成四路统一 3s 轮询(8/17 分页化后各带页参))
- [x] 2.2 项目列表(整行,紧跟 KPI;只读 + 调度启停 `adminToggleSchedule`(确认弹窗,双向切换),无编辑无删除):名称/管理员(`adminName`,0.5 已就绪)/分支/enabled。(8/16;列定稿:项目名称(居中)/项目管理员(居中)/仓库链接(居中,owner/repo 短形可点跳,复用 `libs/dev.ts` 的 `repoShort`)/仓库分支(居中)/单轮 Sonar 处理上限/单轮 AI 处理上限(单轮上限拆两列,居中,title 悬浮完整含义"单轮任务处理的 SonarQube/AI 发现问题数上限")/调度启停(居中,启用/停用)/操作(居中);**8/16 接口定稿**:`POST /dev/admin/project/{id}/schedule` 双向切换(权限 `dev:project:schedule`,取代单向 `disable` + `dev:project:disable`),按钮随当前状态显示"停用调度/启用调度"(四字口径与我的项目页一致,启停整组样式写 scoped scss `schedule-toggle-btn--stop/--start`),弹窗标题/按钮/提示随方向变化);**8/17 服务端分页**:每页 10 条按接入时间倒序 + PageSwitcher 放"项目列表"标题行右侧)
- [x] 2.3 当前任务(8/16 晚由"正在运行"正名):每个进行中 run 一张卡(项目名、触发方式、迷你状态机、秒级 H:MM:SS 实时已耗时——状态机/耗时数据源同 0.9 `actionAttemptBriefs`,列表出口已带,`fmtElapsed` 与我的项目页同口径;**8/17 起数据源为 `statuses=RUNNING,CANCELING` 首页大页快照**,覆盖并发槽 + 排队)+ 管理员"取消"(`adminCancelRun`)。(8/16,mini 版 `PipelineStateMachine`;PENDING 卡显示"排队中"徽章、无取消按钮;取消走确认弹窗;CANCELING 卡卡头补"取消中"琥珀徽章(8/16 晚——此前取消按钮消失后卡头无任何指示,仅迷你条底部小字,感知太弱))
- [x] 2.4 最近完成任务列表(`getAllRun` 终态过滤,放最后):~~取终态前 8 条,倒序~~ **8/17 改服务端分页**:statuses 终态过滤 + 每页 8 条倒序 + PageSwitcher 放"最近完成"标题行右侧(分页后前端 slice/过滤口径废弃)。(8/16;**8/16 晚定稿:与历史任务表同口径**——列=项目(居中,"已删除项目"虚线框不变)/触发方式(手动/调度)/运行结果/扫描发现问题数/已交付修复数/PR/开始时间/结束时间/任务耗时,除耗时右对齐外全居中,`table-fixed` + colgroup 14/8/9/10/10/13/13/13/10,xl 以下隐藏开始/结束时间;任务编号不上表,跨项目视角以项目名锚定)
- [x] 2.5 入口与路由守卫:permissions 含 `dev:run:cancel`/`dev:project:schedule` 才可见可进。(8/16;**实现改为角色驱动**——`Role.DEV_ADMIN`(`ROLE_dev_admin`,8/16 补进 `constants/auth.ts`)控制导航显隐与路由守卫(meta.permissions=[Role.DEV_ADMIN]),Role 与 Permission 同入 permissions 数组由 `hasAnyPermission` 统一判定;两权限点退居按钮级:取消需 `dev:run:cancel`、调度启停需 `dev:project:schedule`)

## 3. 平台介绍页(`/dev/about`)

- [x] 3.1 Hero + 流水线怎么跑(7 阶段说明,序号圆全橙色统一,无执行者徽章——8/16 晚去掉,标签信息密度低且与描述重复;6/7 序号圆原灰色已统一为橙)。(8/16,`AboutView`)
- [x] 3.2 安全三卡(凭证隔离/最小权限/出站白名单)+ 三层身份(owner/bot/平台管理员)。(8/16;owner 正名为**项目管理员**——很多时候仓库 owner 是组织,"归属人/owner"措辞废弃)
- [x] 3.3 三步接入引导 + CTA 跳"我的项目"。(8/16;8/16 晚文案定稿:去掉写死的"每晚 21:00",改"每晚调度时段自动开跑"——具体时间唯一展示点是顶栏胶囊,调 cron 不产生文案漂移)

## 4. 后置(演示后)

- [ ] 4.1 run 实时状态机流转(SSE/WS 推送替代轮询)。
- [ ] 4.2 修复成功率、节省人月(20min/bug)。
- [ ] 4.3 高频缺陷 Top。
