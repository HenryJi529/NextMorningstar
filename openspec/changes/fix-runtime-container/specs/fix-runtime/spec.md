# fix-runtime 规格

## 目的

为每次漏洞修复运行提供隔离、可驱动、可清理的标准化容器环境。

## 需求

### 需求:运行隔离容器

#### 场景:启动运行容器

- **WHEN** 运行进入容器启动阶段
- **THEN** 创建并启动一个独立容器,挂载专属工作区 `/workspace/<runId>`
- **AND** 将容器 ID 记录到对应 run

#### 场景:清理容器

- **WHEN** 运行结束(成功/失败/取消)
- **THEN** 删除该运行对应的容器
