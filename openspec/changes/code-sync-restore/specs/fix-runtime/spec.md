# fix-runtime 规格(增量)

## 需求

### 需求:工作区代码同步与还原

#### 场景:同步目标仓库

- **WHEN** 运行进入仓库同步阶段
- **THEN** 在容器工作区克隆目标仓库(含 submodule)并切到配置分支

#### 场景:失败还原

- **WHEN** 运行失败或取消进入还原阶段
- **THEN** 工作区还原到初始克隆状态,丢弃所有未提交改动
