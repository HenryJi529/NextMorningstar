bindAddr = "0.0.0.0"
bindPort = 7000

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "frps"
webServer.password = "{{ .Envs.FRPS_PASSWORD }}"

auth.method = "token"
auth.token = "{{ .Envs.FRPS_TOKEN }}"

log.to = "console"
log.level = "info"
log.maxDays = 30

transport.tls.force = false
