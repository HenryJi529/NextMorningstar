export enum LocalStorageKey {
    ACCESS_TOKEN = 'access-token',
    BLOG_PREFER_THEME = 'blog-prefer-theme',
    DEVICE_ID = 'device-id',
    GITHUB_OAUTH_STATES = 'github-oauth-states',
}

export enum SessionStorageKey {
    PRE_ROUTE = 'pre-route',
}
